# 城市靈魂 — 雙層地圖圖磚技術規格

> **狀態**：草案,待身體(S7)、臉(F1/F2)、Lead 三方對接確認後定案
> **對應既有文件**：`SDD_v2.1_Unity_3D.md` §5.7(地圖底圖)、§16(尚未解決的缺口:Unity tile renderer 圖磚載入/快取策略)
> **本文件定位**：補完 §5.7 與 §16 留白的部分,不推翻既有決策
> **與角色資產產線的關係**：本文件涵蓋的是**地圖底圖**產線(OSM → Fooocus),與 §7 角色資產產線(ComfyUI/Hunyuan3D → Mixamo)是**完全獨立**的兩條線,互不共用工具鏈、互不影響排程

---

## 0. 範圍與核心原則

### 0.1 為什麼要分層

Home page 地圖需要橫跨的縮放範圍,從城市總覽(玩家瀏覽全大台北尋找地標)到街道細節(50m 召喚範圍內辨識建築物),以標準 web tile zoom 等級估算約橫跨 zoom 11–19,超過三個數量級的解析度差距。單一風格無法同時滿足「遠景要氛圍、近景要可辨識」兩種需求,因此拆成兩種美術風格分層處理。

### 0.2 兩個容易混淆的概念,先拆開

| 概念 | 定義 | 本案決策 |
|---|---|---|
| **風格層** | 用哪一種美術方向(水彩 vs 結構) | 兩種:遠景水彩層 / 近景結構層 |
| **解析度層(z 階)** | tile pyramid 的縮放層級(z/x/y) | 沿用標準 web tile 概念,每個風格層底下可以有多個 z 階 |

不要把「風格層數」跟「z 階數」混為一談——風格只有兩種,但每種風格底下各自涵蓋數個 z 階的圖磚解析度。實際定案為每個風格層各自涵蓋兩個 z 階,共四個 zoom 停靠點,詳見 §0.4。

### 0.4 四個 Zoom 停靠點與風格層對應(★UI 線框已定案)

| Zoom | 名稱 | 視覺特徵 | 所屬風格層 | 生產範圍 |
|---|---|---|---|---|
| 1/4 | 城市全景 | 純水彩色塊,無結構線,僅河流/山脈/城市輪廓 | 遠景水彩層 | 全大台北 |
| 2/4 | 行政區 | 水彩底,路網清晰,**已上線地標以固定圖示標示** | 遠景水彩層 | 全大台北 + 地標圖示(規則見下) |
| 3/4 | 街區 | 建築具立體結構,質感偏軟 | 近景結構層 | 僅已生產近景資料的地標半徑範圍 |
| 4/4 | 建物 | 完整等角結構,單棟建築可辨識 | 近景結構層 | 同上 |

**真正的風格 crossfade(§3 shader 混合邏輯)只發生在 zoom 2 → zoom 3 之間。** zoom 1↔2 與 zoom 3↔4 屬於同一風格層內的解析度過渡,理論上可用單純的 mipmap/LOD 處理,不需要套用跨風格的 blend 邏輯,比原先設想的「每層各自無限 z 階都要 blend」製作成本更低。

**Zoom 2 地標圖示的顯示規則(★資料驅動,非固定清單)**:圖示只在該地標**已有近景結構層資料**時才顯示,不是預先列出全部 9 個地標的清單圖示。理由:
- 與 §0.3 局部生產策略邏輯一致——地標圖示的存在與否,直接反映近景資料是否已生產,不需要另外維護一份「已上線地標清單」跟資料層各自為政
- 新地標近景資料生產完成後,zoom 2 會自動出現該地標圖示,不需修改程式碼或另行設定,呼應 AC15.5 的擴充原則
- 實作上,圖示顯示邏輯可以直接查詢近景結構層 tile(§2.2 命名 `tile_near_{landmark}_*`)是否存在對應 Addressables bundle 作為判斷依據

### 0.3 涵蓋範圍決策(★核心,已與 Lead 確認)

| 風格層 | 覆蓋範圍 | 生產節奏 |
|---|---|---|
| **遠景水彩層** | **全大台北**,一次性全區生產 | 獨立於地標擴充排程,可提前生產 |
| **近景結構層** | **僅已上線地標的半徑範圍**(建議 500m–1km),島狀局部覆蓋 | 跟隨地標擴充節奏(§13.9),目前僅需龍山寺一份 |

**設計理由**:近景結構層需要的等角擠出(Step 2)、逐格上色(Step 4)、切磚接縫處理(Step 5)是整套產線裡成本最高的環節。若要求全市覆蓋,大部分區域玩家永遠不會拉近去看(MVP 僅一個召喚點),屬於浪費產能。局部覆蓋讓近景層的生產量與角色擴充排程綁在同一個開關上——**封測後才生產地標 #2–10 的近景資料,程式碼邏輯不需修改**(呼應 AC15.5 精神)。

範圍外區域,使用者最多拉到遠景水彩層的解析度上限,不會露出破綻,因為那些區域本來就沒有召喚點,沒有拉近的動機。

---

## 1. 資產產線 SOP

### 1.1 產線總覽

```
OSM 向量資料(建築足跡、路網、水系、綠地)
        │
        ├──────────────┬──────────────
        ▼                              ▼
   平面結構控制圖                  等角擠出結構控制圖
   (top-down outline)             (45° isometric, 依樓層擠出)
        │                              │
        ▼                              ▼
   PyraCanny 線稿萃取              PyraCanny 線稿萃取
        │                              │
        ▼                              ▼
   Fooocus(水彩權重高)             Fooocus(結構權重高)
        │                              │
        ▼                              ▼
     切磚 + 接縫處理                切磚 + 接縫處理
        │                              │
        ▼                              ▼
   遠景水彩層 tile                近景結構層 tile
```

**共用的是 OSM 幾何資料源,不是共用同一張線稿**——兩層各自從原始向量資料出發,分別產生控制圖,因為近景層需要多一道等角擠出的 3D 前處理,平面版無法直接沿用。兩者的建築物位置、比例基準一致(都源自同一份 OSM 資料),因此邊界能對齊,但線稿本身是兩份獨立產出。

### 1.2 Step-by-step

#### Step 1 — 資料擷取

- 來源:Overpass API 或下載對應區域的 `.osm.pbf`
- 取出圖層:建築足跡(building footprint)、道路網、水系、綠地/公園
- **保留 `building:levels` 標籤**——這是近景結構層等角擠出的高度依據,若原始資料缺此欄位需要有預設值/估算規則(建議依建築類型給預設樓層數,而非統一高度)

#### Step 2 — 結構控制圖生成(兩層分岔點)

| | 遠景水彩層 | 近景結構層 |
|---|---|---|
| 控制圖類型 | 平面線稿(top-down) | 等角擠出線稿(45° isometric) |
| 產生方式 | 多邊形描邊、直接栅格化 | 依 `building:levels` 將 footprint 擠出成立方體,架設固定 45° 斜角相機渲染出線稿 |
| 建議工具 | QGIS / Mapnik 等平面渲染器 | Blender(或腳本化 3D pipeline)+ 固定機位渲染 |
| 產出深度資訊 | 無 | 有(建築體積、陰影面方向) |

⚠️ **近景結構層必須經過 3D 前處理**,不能只靠 2D 邊緣偵測(Canny)模擬立體感——Diffusion 模型沒有深度資訊可以引導畫出正確的陰影面,若省略這一步,近景層會失去 v2 參考圖那種「看得出建築體積」的效果,只會產出偽裝成立體的平面貼皮。

#### Step 3 — PyraCanny 邊緣萃取

兩層的控制圖各自跑一次 PyraCanny,取得乾淨的多尺度邊緣線稿,作為後續 ControlNet 輸入。

#### Step 4 — Fooocus + ControlNet 上色

固定 prompt 模板 / LoRA / seed(由 FA-8 生成規範統一管理),兩層使用不同的 ControlNet 權重與 denoise 強度:

| 參數 | 遠景水彩層 | 近景結構層 |
|---|---|---|
| Denoise 強度 | 高(水彩自由度大) | 低(確保建築邊界銳利、位置不飄移) |
| ControlNet 權重 | 低(結構只當弱引導) | 高(嚴格依線稿走) |

⚠️ **FA-8 生成規範需要補寫雙層對齊約束**,目前規範僅涵蓋單層,需明確要求兩層的結構基準(seed 中的位置/比例相關參數)保持一致,避免建築物在兩層間的相對位置偏移。

#### Step 5 — 切磚與接縫處理(★最高風險環節)

Diffusion 模型不天生具備無縫拼接能力,直接切割大圖成標準 tile size(建議 256×256 或 512×512)會在邊界產生色塊斷層、線條不連續。

建議處理方式:
- 以較大範圍整圖生成後再切割,避免逐格生成造成同批 tile 內部風格不一致
- 每個 tile 生成時預留重疊區(overlap padding),裁切時只取中心區域,邊界以重疊區做羽化混合
- **此環節建議另立一個 POC 驗證**,目前 SDD §13.2 定義的五個 Phase 0 POC 都未涵蓋此風險,建議併入 P0-A(資產可行性)一併驗證,或另開一項

#### Step 6 — Unity 匯入

依 §2 的 tile 命名規則,各層 tile 分別打包進 Addressables。

## 1.3 地標局部範圍圖磚數量估算(以龍山寺 500m 半徑為例)

近景結構層(zoom3+4)僅針對已上線地標的局部範圍生產(§0.3),實際要切幾格圖磚,可依以下方法估算。**遠景水彩層(zoom1+2)不受此計算影響**——它是全市統一生產,任何地標所在區域早已涵蓋在城市級圖磚內,不需要為個別地標另外多切。

### Step A — 決定涵蓋範圍(用外接方形,不用圓形)

遊戲地圖畫面本身是矩形,玩家平移/縮放時看到的是方形視窗,因此半徑範圍直接用**外接方形**當生產範圍,不需要真的按圓形邊界切割:

- 500m 半徑 → 直徑 1000m × 1000m 正方形,以召喚點為中心
- 建議再加邊界緩衝(考慮 Step 5 切磚時每格需多截一圈做接縫羽化),實際生產範圍抓 **1100m × 1100m**

### Step B — 決定「每格圖磚代表多少實際公尺」

這是計算的關鍵變數,**目前無實測數據,先用合理假設起算,待 POC 驗證後調整**。判斷依據是「單棟建築物在畫面上要佔多少像素才夠清楚」。台北市區一般建築基地約 20–60 公尺見方,若希望單棟樓在圖磚上至少佔 50–100 像素:

| 層級 | 假設:每格涵蓋實際範圍 | 假設:圖磚解析度 | 換算像素密度 |
|---|---|---|---|
| zoom 3(街區) | 100m × 100m | 512×512 px | 約 5 px/m,看得出街廓輪廓 |
| zoom 4(建物) | 50m × 50m | 512×512 px | 約 10 px/m,看得出單棟建築細節 |

### Step C — 算格數

公式:`格數(單邊) = 生產範圍邊長 ÷ 每格代表公尺數`(無條件進位),總格數為單邊格數的平方。

**龍山寺 500m 半徑範例**:

| 層級 | 生產範圍邊長 | 每格代表 | 單邊格數 | 總格數 |
|---|---|---|---|---|
| zoom 3(街區) | 1100m | 100m | 11 格 | 121 格 |
| zoom 4(建物) | 1100m | 50m | 22 格 | 484 格 |

**單一地標近景結構層合計約 600 格圖磚**(121 + 484)。

### 使用此估算時的三個提醒

1. **格數不等於生成次數**:建議依 Step 5 做法,先生成大範圍整圖(如數張 2048×2048 大畫布)再切割,而非逐格單獨呼叫 Fooocus——實際生成成本應以「大畫布張數」估算,遠低於最終 tile 張數,格數估算主要用途是預估 Unity 端匯入與 Addressables 管理的資料量。
2. **每格公尺數為假設值,非定案規格**:實際數值需綜合考量 Fooocus 單張生成解析度上限(GPU 記憶體限制)、Unity 中階手機貼圖效能負荷(呼應 §11.4 硬體限制)、美術實測後的可讀性判斷,待 POC 階段一併驗證後回填定案數值。
3. **多地標擴充時的量級參考**:若 9 個地標均比照此規模,總量級約 600 格 × 9 ≈ 5400 張圖磚,但依 §13.9 排程,此非目前需處理範圍,現階段僅需龍山寺一份約 600 格的完整流程跑通即可。



### 2.1 定址方式

比照標準 web tile pyramid(z/x/y)概念,但 z 軸只分兩段風格區間,不是連續無限層級:

| UI Zoom 停靠點 | 對應 z 範圍(示意) | 風格層 | 涵蓋範圍 |
|---|---|---|---|
| 1/4 城市全景 | z ≈ 11–12 | 遠景水彩層 | 全大台北 |
| 2/4 行政區 | z ≈ 13–14 | 遠景水彩層 | 全大台北 + 地標圖示(§0.4 資料驅動規則) |
| 3/4 街區 | z ≈ 15–17 | 近景結構層 | 僅已生產近景資料的地標半徑範圍 |
| 4/4 建物 | z ≈ 18–19 | 近景結構層 | 同上 |

實際 z 分界值待身體(S7)依 Unity camera 參數與美術可讀性測試後微調,此處為預估區間。四個停靠點是否為連續縮放中的離散吸附點(snap zoom),或允許連續縮放,屬 UI 互動設計範疇,不在本文件定案範圍內,建議另與 Lead / 敘事討論。

### 2.2 Addressables Key 命名

```
tile_{layer}_{z}_{x}_{y}

範例:
tile_wide_12_3421_1567      ← 遠景水彩層
tile_near_longshan_17_3421_1567   ← 近景結構層,含地標識別碼
```

近景結構層的 key 額外帶地標識別碼(如 `longshan`),原因:
- 便於依地標分別管理生產進度與版本更新,不影響其他地標
- 未來新增地標 #2–10 時,只需新增對應 key 的 bundle,不需重構既有命名結構

---

## 3. Shader Crossfade 技術規格

### 3.1 設計哲學

與 §9.2 靈魂 shader 的「效果與資產解耦」設計哲學平行——crossfade 邏輯完全在 shader 層,不需要重新載入或替換底層資產,只是依相機縮放狀態即時混合兩張已載入的 tile 貼圖。

### 3.2 Shader Graph 邏輯

**★ 跨風格的 blend 只發生在 zoom 2(行政區)→ zoom 3(街區)之間**,對應 §0.4 的四階定義。zoom 1↔2 與 zoom 3↔4 屬同一風格層內的解析度過渡(mipmap/LOD 層級,非跨風格 blend),不套用本節邏輯,可用較輕量的方式處理,細節留給 S7 依 Unity 內建 LOD 機制評估。

```
Sample Tex A(遠景水彩層 tile,zoom 2 版本)
Sample Tex B(近景結構層 tile,zoom 3 版本,若該範圍內存在)
blendFactor = smoothstep(zoomNear, zoomFar, cameraOrthoSize)
最終色 = lerp(TexB, TexA, blendFactor)
```

- `blendFactor` 由 camera 的 orthographic size(或等效的 zoom 距離參數)即時驅動,單一 material property,不需每幀重新載入資源
- 交界帶(zoom 2 ~ zoom 3 之間)內,兩層 tile 同時常駐記憶體;出了交界帶後,非目前使用的那一層 tile 應從 Addressables 釋放
- 此設計比「每個風格層各自無限 z 階都要 blend」的原始設想成本更低——只有一個交界點需要處理跨風格運算

### 3.3 範圍判定邏輯(★結合 §0.3 局部生產策略)

```
若 camera 位置落在「已生產近景資料」的地標半徑內:
    依 zoom 做遠景/近景 crossfade(§3.2 邏輯)
否則(範圍外,尚無近景資料):
    僅顯示遠景水彩層,近景 tile 不嘗試載入
```

此判定邏輯不依賴地標數量寫死,新地標的近景資料補齊後,只需新增對應 Addressables bundle,程式碼邏輯不需修改——呼應 AC15.5「正式角色替換灰模時不需修改任何程式碼」的同一個擴充原則。

---

## 4. 跨模組介面(身體 S7 / 臉 F1-F2 邊界)

| 交付物 | 負責 | 說明 |
|---|---|---|
| OSM 資料擷取 + 結構控制圖生成(平面版 + 等角擠出版) | 臉(F1/F2 延伸) | 需要 Blender 或腳本化 3D 前處理能力,與現有 F2「Blender 清理管線」性質相近,建議由同一人力承接 |
| PyraCanny + Fooocus 生成 + 切磚 | 臉 | 沿用既有 ComfyUI 環境外的 Fooocus 工具鏈,§7.1 已提及但目前規範僅涵蓋角色,需擴充 |
| Tile pyramid 載入邏輯 + Addressables 管理 + LRU 快取 | 身體(S7) | 比照角色資產「進入 150m 感應範圍才下載」原則(AC15.4),tile 只載可視範圍 + 緩衝圈,離開範圍後 LRU 釋放 |
| Shader crossfade(§3) | 身體(S7)或臉(F4 對應人力) | 邏輯與靈魂 shader 平行,建議由熟悉 §9.2 那套 shader graph 的人力一併處理,避免兩套視覺系統各自為政 |

⚠️ **此工作項目目前未對應到既有 WBS 編號**,建議 Lead 依現有 S7/F1/F2 補開子項或新增獨立票號,並確認工時是否需要重新估算——本文件涵蓋的生產範圍(遠景全市 + 近景僅龍山寺局部)已刻意收斂,理論上不至於大幅超出 S7/F1/F2 原本的工時假設,但等角擠出與接縫處理是原規劃未明確提及的新工作量。

---

## 5. 風險與待驗證項目

| 風險 | 說明 | 建議處理 |
|---|---|---|
| **切磚接縫** | Diffusion 生成天生不具備無縫拼接能力,是全流程風險最高的環節 | 建議獨立 POC,或併入既有 P0-A 資產可行性驗證 |
| **等角擠出可行性** | 近景結構層依賴的 3D 前處理步驟,目前排程未明確涵蓋此工作量 | 建議先以龍山寺方圓 300m 小範圍跑一輪完整流程驗證,再決定是否投入全地標生產 |
| **雙層風格對齊** | 遠景/近景兩層若結構基準飄移,crossfade 轉場會出現建築物位置跳動 | FA-8 生成規範需明確補上對齊約束(§1.2 Step 4) |
| **z 分界值调校** | 目前 zoom 分界為預估值,實際需依 Unity camera 參數與可讀性測試調整 | 待 S7 開發時實測 |

---

## 5.1 粗估工時(★不確定性高,建議先跑探索性 milestone)

以下估算依專案既有 3 天/Sprint 節奏拆解,精確度受限於兩個目前完全沒有實測數據的環節(等角擠出、切磚接縫)——這兩項連建置階段本身都算是驗證,類似 §7.1 角色產線需要 Phase 0 POC 才敢排時程的情境,地圖圖磚目前連這一步都還沒跑。

| 工作項 | 性質 | 估算(Sprint) | 依據/風險 |
|---|---|---|---|
| OSM 資料擷取 + pipeline 腳本建置(兩層共用) | 一次性 | 0.5–1 | 技術成熟,主要是寫腳本跟抓資料範圍 |
| 平面控制圖生成(zoom1/2 用) | 一次性,全市 | 0.5 | Mapnik/QGIS 屬既有工具,風險低 |
| **等角擠出 pipeline 建置**(zoom3/4 用) | 一次性(建好後可重複套用) | 1–2 | ⚠️ 全新工作量,無前例可套,建置階段本身即為驗證 |
| PyraCanny + Fooocus 生成規範調校(FA-8 雙層對齊) | 一次性 | 1 | 需反覆試 seed/LoRA/prompt 才能讓兩層結構基準對齊 |
| **切磚接縫處理驗證**(獨立 POC) | 一次性 | 1 | ⚠️ 全流程最高風險項,可能需重跑多次 |
| 遠景水彩層(zoom1+2)全市正式生產 | 一次性,全市 | 1–2 | 範圍大但風格單純,批次跑,人力主要在把關品質 |
| 近景結構層(zoom3+4)龍山寺局部生產 | 每地標重複一次 | 0.5–1 | 局部小範圍(500m–1km),pipeline 抓穩後應較快 |
| S7 tile pyramid 載入 + LRU 快取 | 一次性(架構) | 1 | 沿用既有 Addressables 經驗(§5.7 已有底子) |
| S7 shader crossfade(zoom2→3) | 一次性 | 0.5–1 | 邏輯與靈魂 shader(§9.2)平行,若同一人力承接會更快 |

**粗抓總量**(樂觀情境,假設臉能承接等角擠出/Fooocus 調校、身體有餘裕做 crossfade):

- 產線建置 + POC 驗證(前四項風險項):約 **3.5–5 Sprint**(10.5–15 工作天)
- 正式生產(遠景全市 + 龍山寺局部):約 **1.5–3 Sprint**(4.5–9 工作天)
- Unity 端整合(S7):約 **1.5–2 Sprint**(4.5–6 工作天)
- **合計約 6.5–10 Sprint,即 20–30 個工作天(4–6 週)**

**準確度取決於兩件事**:
1. 等角擠出能否順利建置——若 Blender 腳本化流程卡關,或渲染線稿餵給 Fooocus 後效果不如預期,單這一步就可能吃掉 2–3 個 Sprint 反覆調整
2. 切磚接縫的實際嚴重程度——若 overlap padding + 羽化混合能解決則 1 Sprint 足夠;若需換更複雜方案(如大範圍整圖切割取代逐格生成),可能翻倍

**建議處理方式**:比照 §13.2 Phase 0 POC 的精神,把「產線建置 + POC 驗證」這 3.5–5 Sprint 當作**獨立的探索性 milestone** 先跑,跑完拿到實測數據後,才將正式生產與 S7 整合的數字排入正式 Sprint 表,不建議在完全沒有實測基準的狀態下把整個估算直接排進去。



- 近景結構層地標半徑的精確數值(本文件暫以 500m–1km 為建議值)
- 等角擠出 3D 前處理由臉的哪個角色承接(F1/F2 既有定義未明確涵蓋此項)
- Phase 排程中,此項工作應併入現有 Phase 幾(目前 §16 僅標注「S7,Phase 2」,但等角擠出與接縫 POC 若需提前驗證,可能要往前移至 Phase 0/1)
- 與 `brain.districts`(§9,萬華區劇本地理圍欄)是否有資料共用空間——兩者都用到 PostGIS 多邊形概念,但用途不同(districts 是劇情觸發,這裡是視覺圖磚),暫定兩者資料互不相關,若之後有共用需求需另行評估
- 四個 zoom 停靠點(§0.4)是否為離散吸附縮放(snap zoom,UI 上明確卡在四級)或允許連續縮放、僅在特定範圍內觸發風格 crossfade——屬 UI 互動設計範疇,待與 Lead / 敘事確認後回填本文件
- zoom 2 地標圖示的查詢時機:是進入 MapScene 時一次性拉取「已有近景資料的地標清單」,還是即時查詢 Addressables 是否存在對應 bundle——實作細節待 S7 依效能評估決定
